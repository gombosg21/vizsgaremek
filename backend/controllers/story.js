const media_story = require('../models').media_story;

exports.createStory = async (req, res, next) => {
    const userID = req.user.ID;
    try { 

    } catch (error) {
        console.error(error);
        res.status(500);
    };
};

exports.getStory = async (req, res, next) => {
    try { 

    } catch (error) {
        console.error(error);
        res.status(500);
    };
};

exports.editStory = async (req, res, next) => {
    const userID = req.user.ID;
    try { 

    } catch (error) {
        console.error(error);
        res.status(500);
    };
};

exports.deleteStory = async (req, res, next) => {
    const userID = req.user.ID;
    try { 

    } catch (error) {
        console.error(error);
        res.status(500);
    };
};




